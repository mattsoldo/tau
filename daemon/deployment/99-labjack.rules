# LabJack U3-HV USB udev rules
# Copy this file to /etc/udev/rules.d/99-labjack.rules
# Then run: sudo udevadm control --reload-rules && sudo udevadm trigger

# LabJack U3-HV (USB VID:PID 0cd5:0009)
# Allows non-root users in the plugdev group to access LabJack devices
SUBSYSTEM=="usb", ATTR{idVendor}=="0cd5", ATTR{idProduct}=="0009", MODE="0666", GROUP="plugdev"

# Installation:
# 1. Copy this file: sudo cp 99-labjack.rules /etc/udev/rules.d/
# 2. Reload rules: sudo udevadm control --reload-rules
# 3. Trigger: sudo udevadm trigger
# 4. Add user to group: sudo usermod -aG plugdev tau
# 5. Reconnect LabJack USB device
